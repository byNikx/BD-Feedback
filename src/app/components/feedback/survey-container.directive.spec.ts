import { SurveyContainerDirective } from './survey-container.directive';

describe('SurveyContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new SurveyContainerDirective();
    expect(directive).toBeTruthy();
  });
});
